# Unitree Go2 基于视频参考路线的 NWM 真机实验

> 协议版本：0.1.0；状态：测试前草案；适用仓库：CompACT
>
> 任务：Go2 根据 Go2 或人类手机采集的参考视频，闭环跟随同一条视觉路线
>
> 原则：先冻结协议，再运行正式测试；不得用正式测试结果调参

## 1. 实验目标

由于NWM目前只能做图像目标导航，因此本实验回答三个递进问题：

- **Q1：同载体复现。** Go2 能否根据自己预先采集的视频，闭环复现参考路线？
- **Q2：受控跨载体迁移。** Go2 能否根据人类在接近机器人相机高度采集的视频跟随路线？
- **Q3：自然跨载体迁移。** Go2 能否根据人类正常手持手机采集的视频跟随路线？

对应假设：

- **H1：** Go2 自采参考视频可以支持闭环路线复现，且一步重规划优于连续执行 8 步后再重规划。
- **H2：** 低视角手机参考视频可以替代 Go2 自采视频，且性能下降有限。
- **H3：** 自然手持视频仍能支持路线跟随，但受视角差异影响，性能低于低视角视频。

上述内容是待验证假设，不是预期结论。正式结果必须包含失败试验。

## 2. 任务定义与边界

### 2.1 任务输入

导航系统只允许使用：

- 一段有序的参考 RGB 视频；
- Go2 最近 4 帧前视 RGB 图像；
- NWM 生成局部候选动作；
- Go2 里程计，仅用于执行 NWM 给出的局部相对位移；
- 激光雷达或深度传感器，仅用于安全过滤和紧急停车。

全局位姿、参考路线坐标和终点真值不得输入视觉路线定位器或 NWM。独立定位系统只用于计算评测指标。

### 2.2 “跟随同一路线”的定义

本任务不要求机器人逐厘米复制参考轨迹，而要求：

1. 按正确顺序经过参考路线；
2. 不离开预先定义的可行走区域；
3. 无碰撞、跌倒和人工接管；
4. 在时间限制内到达终点位置和朝向阈值。

因此，本实验属于视觉路线跟随或视觉 teach-and-repeat，不是精确轨迹跟踪。

### 2.3 不允许的结果表述

- 只在录制数据上离线规划，不能称为真机闭环导航。
- 使用测试轨迹调参后，不能称为未见测试。
- 使用手机位姿在线给机器人导航，不能称为仅依赖手机视频。
- 只报告成功案例，不能作为成功率结果。

## 3. 系统组成

```text
参考视频 -> 顺序参考帧库 ---------------------------+
                                                    |
Go2 RGB -> 当前路线位置匹配 -> 前方目标帧 -> NWM+CEM |
                                                    v
独立定位(仅评测)     Go2里程计 -> 局部控制 <- 首步SE(2)动作
                                         |
激光/深度 -> 安全过滤与watchdog ----------+-> Go2高层运动接口
```

系统必须拆分为以下模块，并分别记录版本：

1. **视频预处理器：** 解码、定频采样、裁剪、缩放和归一化。
2. **路线定位器：** 在参考帧的局部窗口内定位当前进度。
3. **目标管理器：** 选择当前进度前方的目标帧。
4. **NWM 规划器：** 根据最近观测和候选动作预测未来图像并选择动作。
5. **局部控制器：** 将相对位移转换为 Go2 高层速度命令。
6. **安全监督器：** 限幅、避障、超时停车和人工急停。
7. **评测记录器：** 保存图像、位姿、动作、匹配结果、延迟和事件。

NWM 输出是相对位移，不是速度命令。在线执行时应取规划序列第一步：

```text
模型动作:   (dx_unit, dy_unit, dyaw)
米制动作:   (spacing * dx_unit, spacing * dy_unit, dyaw)
局部目标:   T_target = T_current * SE2(dx_meter, dy_meter, dyaw)
```

局部控制器再将 `T_target` 转换为限幅后的 `(vx, vy, vyaw)`。不得把 NWM 位移直接传给速度接口。

## 4. 实验场地与路线

场地中设置四条互不重叠的路线。实际坐标、照片和长度写入 `routes.yaml`。

| 路线 | 用途 | 建议长度 | 几何结构 | 是否进入正式结果 |
|---|---|---:|---|---|
| C0 | 控制与坐标校准 | 5 m | 直线、空旷 | 否 |
| D0 | 开发和参数选择 | 10–15 m | 一个 90° 转弯 | 否 |
| T1 | 正式测试 | 12–18 m | 直线加一个转弯 | 是 |
| T2 | 正式测试 | 20–30 m | 两个转弯和一个窄通道 | 是 |

路线要求：

- C0、D0、T1、T2 不共享连续路段；
- 正式实验期间路线结构保持不变；
- 起点、终点和参考中心线在独立评测坐标系中固定；
- 标记尽量位于相机视野外，避免成为视觉捷径；
- 核心实验只使用静态、平整、安全的室内环境；
- 光照变化、动态障碍和不平地面只进入扩展实验。

## 5. 参考视频采集

### 5.1 三种参考源

| ID | 参考源 | 采集方式 | 作用 |
|---|---|---|---|
| R | Go2 自采 | 人工遥控 Go2 沿中心线行走 | 同视角上界 |
| H-low | 人类低视角 | 手机固定在接近 Go2 相机的高度和朝向 | 受控跨载体 |
| H-natural | 人类手持 | 手机横屏、自然手持，但始终朝向路线前方 | 自然跨载体 |

每条正式路线、每种参考源采集 **3 条相互独立的有效参考视频**，编号为 `take-1` 至 `take-3`。三次视频必须重新从起点开始采集，不能由一条长视频切分得到。D0 每种参考源至少采集 1 条。

参考采集顺序使用固定随机种子 `20260907` 生成并保存。若出现中断、明显偏离路线或视频损坏，必须在任何对应测试开始前重采，并保留废弃记录；不得根据机器人后续表现挑选参考视频。

### 5.2 统一采集要求

- 三种参考源沿同一中心线、同一起点、同一终点和同一终点朝向行走；
- 参考平均速度控制在 `0.25 ± 0.05 m/s`；
- 原始视频不低于 15 Hz，保留原始分辨率和时间戳；
- 手机使用横屏、固定镜头、关闭数字变焦；
- 不主动左右扫视，转弯时镜头随行进方向连续转动；
- 记录相机型号、镜头、分辨率、帧率、安装高度、俯仰角和内参；
- 记录参考采集开始和结束时间、光照、人员及异常事件；
- 手机视频可附带 IMU/VIO，但这些信息只能用于离线审计和评测，不能输入主方法。

### 5.3 确定性预处理

所有参考视频使用同一程序处理：

1. 按时间戳重采样到 4 Hz；
2. 进行仓库一致的 4:3 中心裁剪；
3. 缩放为 224×224；
4. 使用 checkpoint 对应的图像归一化；
5. 按时间顺序编号，不删除模糊帧；
6. 输出帧列表、时间戳和文件 SHA-256。

模糊帧只能作为整段参考视频的质量控制依据，不能在看到测试结果后选择性删除。正式参数冻结前，应在 D0 上确定允许的模糊程度和是否需要重采。

## 6. 在线路线跟随方法

### 6.1 参考进度定位

设参考帧为 `G[0...N-1]`，当前进度为 `k`：

1. 初始化 `k=0`；
2. 在固定窗口 `G[k-back : k+forward]` 中计算当前 Go2 图像与参考帧的 DINO 距离；
3. 选择距离最小的帧 `k_match`；
4. 更新 `k=max(k, k_match)`，保证进度不后跳；
5. 目标帧为 `G[min(k+lookahead, N-1)]`；
6. 连续匹配失败超过阈值时停车，不得用真值位姿纠正 `k`。

环形路线或高度重复的走廊不进入核心实验。它们需要额外的分段约束，属于扩展实验。

### 6.2 NWM 重规划

每次规划执行：

1. 读取最近 4 帧 Go2 图像；
2. 使用 CEM 生成候选局部动作；
3. NWM 预测每个动作后的终点图像；
4. 用冻结的视觉距离比较预测图像和目标帧；
5. 安全监督器剔除不可执行或可能碰撞的动作；
6. 执行最优动作的第一步；
7. 获取新的真实图像后重新定位和规划。

核心实验始终采用一步执行、闭环重规划。不得用 NWM 生成图像替代真实相机反馈。

### 6.3 初始参数与冻结规则

下表是 D0 开发实验的初始值。正式测试前将最终值写入独立的 `frozen_config.yaml`，计算 SHA-256 后禁止修改。

| 参数 | 初始值 |
|---|---:|
| 模型输入帧率 | 4 Hz |
| 上下文长度 | 4 帧 |
| 目标前视 `lookahead` | 8 帧 |
| 定位窗口 `back/forward` | 4 / 16 帧 |
| 规划 horizon | 8 步 |
| CEM candidates / top-k | 80 / 5 |
| CEM 优化轮数 | 1 |
| 每候选随机重复数 | 3 |
| 目标距离 | DINO |
| 单步最大平移 | 0.25 m |
| 单步最大转角 | 20° |
| 最大前向/横向速度 | 0.25 / 0.15 m/s |
| 最大角速度 | 0.5 rad/s |
| 控制或通信 watchdog | 0.5 s |
| 最小安全净空 | 0.4 m |

D0 只允许选择以下类别的参数：路线定位窗口、`lookahead`、视觉距离、动作限幅、CEM 预算和停止阈值。不得在 T1/T2 上继续调参。

## 7. 由简单到深入的执行阶段

每个阶段必须通过退出条件后才能进入下一阶段。

### 阶段 0：静态与安全检查

机器人不行走，只验证：

- 相机、里程计、激光和时间戳同步；
- NWM 能读取最近 4 帧并返回有限动作；
- 坐标正负方向经过前进、左移、右转三个小动作验证；
- 超时、断网、图像过期和人工急停均触发 `StopMove`；
- 所有命令、状态和图像写入日志。

退出条件：连续 20 次规划无 NaN/Inf；四种停车条件全部通过。

### 阶段 1：C0 上的 Go2 自采视频

1. Go2 遥控采集 C0 参考视频；
2. 从标准起点执行 3 次闭环路线跟随；
3. 再从左右各偏移 0.3 m 的位置各执行 3 次；
4. 不统计为正式结果。

退出条件：9 次中至少 8 次安全到达；无碰撞、跌倒或失控；单步动作方向正确。

### 阶段 2：D0 开发与参数冻结

在 D0 上采集 R、H-low 和 H-natural 三种参考视频。允许调节第 6.3 节列出的参数。

开发比较至少包括：

- LPIPS 与 DINO 目标距离；
- 一步重规划与每 8 步重规划；
- 三种参考源的路线定位成功率；
- 推理延迟与候选数量的关系。

选择规则必须在运行前写明，例如“首先满足零碰撞，其次最大化到达率，再最小化运行时间”。冻结 checkpoint、配置、代码提交、相机设置和安全参数。

退出条件：三种参考源均至少完成 1 次 D0；生成完整的 `frozen_config.yaml` 和清单 SHA-256。

### 阶段 3：T1/T2 上的正式核心实验

按第 8 节的固定矩阵运行。正式运行期间：

- 不修改模型、参数、路线定位、控制器或安全阈值；
- 不重采参考视频；
- 不根据当前结果选择下一次实验条件；
- 每次运行结束立即保存结果和失败类别。

### 阶段 4：扩展鲁棒性实验

只有阶段 3 完成后才能运行。每次只改变一个因素：

- 光照变化；
- 少量行人或可移动障碍；
- 地毯、草地或缓坡；
- 更高的手机视角或更明显的手机晃动；
- 参考采集与测试之间的时间间隔。

扩展实验必须单独编号，不能与核心静态环境结果混合。

## 8. 正式实验矩阵

### 8.1 起点条件

以参考路线起点切线为 `x` 轴，左侧为 `+y`：

| ID | 横向偏移 | 航向偏移 |
|---|---:|---:|
| S0 | 0 m | 0° |
| S1 | +0.5 m | 0° |
| S2 | -0.5 m | 0° |
| S3 | 0 m | +30° |
| S4 | 0 m | -30° |

每次使用独立定位系统将机器人放置到目标起点容差 `±0.05 m、±3°` 内。导航开始后，独立定位不再输入算法。

### 8.2 核心运行数

```text
2 条正式路线 × 3 种参考源 × 3 条独立参考视频 × 5 个起点 = 90 次
```

| 参考源 | T1 | T2 | 合计 |
|---|---:|---:|---:|
| R | 15 | 15 | 30 |
| H-low | 15 | 15 | 30 |
| H-natural | 15 | 15 | 30 |
| 合计 | 45 | 45 | 90 |

阶段开发顺序必须是 R → H-low → H-natural；正式实验则使用固定随机种子 `20260907` 随机排列路线、参考源、参考视频编号和起点，避免电量、光照和时间顺序混淆。每条参考视频在每个起点只运行一次。生成后的顺序保存为 `episode_schedule.json`，正式运行前冻结其 SHA-256。

### 8.3 闭环对照

在 T1、参考源 R 上增加一个对照：执行 NWM 规划的 8 步后才重规划。使用 3 条参考视频和 S0–S4，每个组合运行一次，共增加 15 次。

主方法的一步重规划结果已经包含在核心 90 次中。该对照只用于检验真实反馈频率的作用，不替代参考源比较。

## 9. 终止条件与指标

### 9.1 单次运行终止

满足任一条件立即终止：

- **成功：** 到达终点 `0.5 m` 内且航向误差不超过 `30°`；
- **超时：** 运行时间超过 `max(120 s, 3 × 对应参考视频时长)`；
- **越界：** 机器人中心离开预定义可行走区域；
- **碰撞或跌倒：** 发生任何环境接触事故或姿态失稳；
- **人工接管：** 安全员发送运动或急停指令；
- **系统失败：** 连续 10 s 无法定位、规划或执行有效动作。

人工接管、碰撞、跌倒和算法超时均计为失败。明确的硬件故障可以标记为无效试验，但必须保留日志，并按原计划位置补跑一次。

### 9.2 主指标

- **Success Rate：** 成功次数 / 全部有效运行次数。

### 9.3 次指标

- 路线完成比例：最大已匹配参考帧索引 / 参考帧总数；
- 最终位置误差和航向误差；
- 相对参考中心线的横向 RMSE 和最大偏差；
- 路径效率：参考路线长度 / 实际执行路径长度，上限截断为 1；
- 完成时间；
- 碰撞、跌倒、人工接管和安全停车次数；
- 路线定位失败次数和持续时间；
- 规划延迟的中位数、P95 和最大值；
- 实际重规划频率和机器人静止等待占比。

路线完成比例属于诊断指标，不能代替到达成功率。

### 9.4 统计报告

- 成功率同时报告原始计数和 Wilson 95% 置信区间；
- 连续指标报告中位数和四分位距，不只报告均值；
- 三种参考源按路线、起点和参考视频编号配对；
- 成功率的两两比较使用精确 McNemar 检验，并对多重比较使用 Holm 校正；
- 另按路线分别报告全部结果，不能只给 90 次总体平均；
- T1/T2 只有两条路线，结论不得外推为任意环境泛化。

## 10. 失败分类

每次失败只能指定一个主类别，并可附加多个次类别：

| 代码 | 类别 | 判定依据 |
|---|---|---|
| LOC | 路线定位失败 | 当前参考索引错误、停滞或跳到错误位置 |
| GOAL | 目标选择失败 | 目标过近、过远或顺序错误 |
| NWM | 世界模型规划失败 | 候选未来预测或动作排序明显错误 |
| CTRL | 执行失败 | 局部控制未实现目标相对位移 |
| SAFE | 安全过滤停止 | 所有候选动作均被拒绝或触发净空限制 |
| VIEW | 跨视角失败 | 手机与 Go2 图像无稳定对应关系 |
| SYS | 系统故障 | 通信、传感器、算力或软件异常 |
| ENV | 环境变化 | 未计划的人、物体或光照变化阻断路线 |

分类由运行日志和视频复核完成。复核者不得修改成功/失败判据。

## 11. 安全协议

- 只使用 Go2 高层运动模式；不进行关节力矩控制；
- 每次运行前检查电量、网络、相机、里程计、激光和遥控急停；
- 安全员全程持有遥控器，并站在机器人运动范围外；
- 初次测试使用软质障碍和空旷区域；
- 图像、控制或通信超过 watchdog 时立即发送停车命令；
- 安全过滤发生异常时默认停车，不沿用上一条速度命令；
- 不关闭机器人原生保护功能；
- 发生碰撞、跌倒或不可解释运动后，完成检查前不得继续下一次运行。

安全员可以随时终止试验。安全优先于补齐实验矩阵。

## 12. 数据与日志

大型数据和结果保存到 NAS，源码和小型配置保留在仓库：

```text
/file_system/nas/algorithm/dujun.nie/nwm/go2_video_route/v0.1.0/
├── references/
│   └── <route>/<source>/<reference_id>/
│       ├── raw_video.*
│       ├── frames/
│       ├── timestamps.csv
│       ├── camera.json
│       ├── environment.json
│       └── manifest.json
├── schedules/
│   └── episode_schedule.json
├── episodes/
│   └── <episode_id>/
│       ├── rgb/
│       ├── observations.csv
│       ├── poses_eval.csv
│       ├── commands.csv
│       ├── planner.jsonl
│       ├── safety_events.jsonl
│       ├── result.json
│       └── manifest.json
└── reports/
    ├── aggregate.json
    ├── aggregate.csv
    └── report.md
```

每个 episode manifest 至少包含：

```yaml
protocol_version: 0.1.0
episode_id: ...
route_id: T1
reference_source: R  # R、H-low 或 H-natural
reference_take: 1  # 1、2 或 3
reference_sha256: ...
start_condition: S0  # S0、S1、S2、S3 或 S4
schedule_sha256: ...
git_commit: ...
checkpoint_path: ...
checkpoint_sha256: ...
frozen_config_sha256: ...
robot_model: Unitree Go2
robot_firmware: ...
camera: ...
started_at_utc: ...
ended_at_utc: ...
termination_reason: success  # 枚举值见第 9.1 节
primary_failure_code: null  # 失败时填写第 10 节代码
```

原始日志只追加、不覆盖。聚合报告必须可以从 episode manifest 和日志重新生成。

## 13. 正式测试前检查表

### 数据与配置

- [ ] C0、D0、T1、T2 的路线坐标、照片和可行走区域已冻结。
- [ ] T1/T2 每种参考源的 3 条独立参考视频均已完成质量检查。
- [ ] 参考帧、checkpoint、配置和代码提交的 SHA-256 已记录。
- [ ] `frozen_config.yaml` 已生成，正式测试后只读。
- [ ] `episode_schedule.json` 已按固定种子生成并冻结。
- [ ] D0 与 T1/T2 不共享路线，且未使用 T1/T2 结果调参。

### 系统与安全

- [ ] 相机、里程计、激光和评测定位时间同步。
- [ ] `dx/dy/dyaw` 坐标方向经过实机小动作验证。
- [ ] 位移单位已由 waypoint unit 正确转换为米。
- [ ] watchdog、网络中断、图像过期和人工急停测试通过。
- [ ] 安全限幅和净空阈值与冻结配置一致。
- [ ] 每次运行可以完整保存并重新读取日志。

### 报告

- [ ] 成功判据和排除规则已在看测试结果前冻结。
- [ ] 失败运行不会从聚合结果中删除。
- [ ] 主指标、次指标和统计方法已经实现并测试。
- [ ] 结果表保持空白，未填写预测值或人工估计值。

## 14. 结果表模板

正式实验完成前不得填写推测数值。

| 参考源 | 路线 | 成功/总数 | Success Rate [95% CI] | 完成比例 median [IQR] | 横向 RMSE | 人工接管 | 碰撞 |
|---|---|---:|---:|---:|---:|---:|---:|
| R | T1 | — | — | — | — | — | — |
| R | T2 | — | — | — | — | — | — |
| H-low | T1 | — | — | — | — | — | — |
| H-low | T2 | — | — | — | — | — | — |
| H-natural | T1 | — | — | — | — | — | — |
| H-natural | T2 | — | — | — | — | — | — |

## 15. 版本与变更规则

- 修改文字、链接或不影响结果的日志字段：增加 patch 版本；
- 修改指标实现、参数或运行顺序：增加 minor 版本，并重新运行受影响实验；
- 修改任务定义、输入信息或成功判据：增加 major 版本，旧结果不得直接合并；
- 所有协议变更写入下表，不修改已完成版本的历史结果。

| 日期 | 版本 | 变更 | 原因 | 受影响实验 |
|---|---|---|---|---|
| 2026-09-07 | 0.1.0 | 初始实验协议 | 建立 Go2/手机视频路线跟随基准 | 全部 |
